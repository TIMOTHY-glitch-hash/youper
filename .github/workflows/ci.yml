name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Repository sanity check
        run: |
          echo "Repository is initialized for collaboration."
          echo "Add build, lint, and test commands once the app stack is selected."
